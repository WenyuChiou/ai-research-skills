# Banned-word audit — Lim et al. 2025 abstract

Generated by `academic-writing-skills` on 2026-04-25, using
`~/.claude/skills/academic-writing-skills/references/banned_words.md` as
the source list. Per the skill's "always audit a paragraph against this
list before showing it to the user" rule.

**Paragraph audited:** the abstract of *Artificial social influence via
human-embodied AI agent interaction in immersive virtual reality (VR)*
(Lim, Schmälzle & Bente 2025), as recorded in
`test-corpus/ai-agents-social-interaction/papers/01-lim-2025-vr-embodied-agents.md`.

## Findings (3 hits, 1 borderline)

| # | Sentence | Hit | Category | Suggested replacement |
|---|---|---|---|---|
| 1 | "We address this gap by **leveraging** latest advances in AI (large language models) ..." | `leveraging` | Banned verb (§1) | "using" / "drawing on" |
| 2 | "We **address this gap** by leveraging latest advances in AI ..." | "address this gap" cliché | Borderline filler / overused phrase | Name the specific gap: "We extend prior text-only CA work by ..." |
| 3 | "Overall, our findings highlight embodiment as a **crucial** factor of AI's influence on human behavior ..." | `crucial` | Banned adjective (§1) | Delete "crucial" or quantify: "embodiment was associated with significantly larger gaze duration (statistic + effect size)" |
| 4 | "Overall, our findings **highlight** embodiment as a crucial factor ..." | `highlight` | Style-discouraged (use sparingly per `underscore` row) | "show", "indicate" |

## What the audit did NOT flag (correctly)

- **"tended to enhance gaze duration"** — `tended to` is on the
  *recommended replacement* list for `ensures` / `must` / `invariably`.
  The paper hedges correctly here.
- **"directional pattern was observed"** — accurate hedge for a
  non-significant trend, not banned.
- **"interaction effect between agent and participant gender"** — proper
  statistical terminology, not the GPT-style filler use of
  "interaction".
- **"our paradigm enables new experimental research"** — `enables` is
  flagged as filler in the phrase "This enables ...", but here the
  subject is a concrete noun ("our paradigm"), so it reads as a real
  claim, not filler.

## Verdict

The Lim 2025 abstract is **mostly clean** by the banned-word standard:
1 banned verb (`leveraging`), 1 banned adjective (`crucial`), 1
borderline noun-phrase (`address this gap`), and 1 style-discouraged
verb (`highlight`). All four are common in journal abstracts and the
paper still reads as substantive empirical work. A real revision pass
would propose: replace `leveraging` with `using`, drop `crucial`, and
replace `highlight` with `show`.

## Skill output footer

```
[academic-writing-skills · banned-word audit]
  Audited: 1 paragraph (172 words, 8 sentences)
  Reference: ~/.claude/skills/academic-writing-skills/references/banned_words.md
  Hits: 3 banned, 1 style-discouraged, 1 borderline filler
  Suggested next: apply the 3 substitutions above; consider quantifying
  "tended to enhance gaze duration" in the body of the paper.
```
