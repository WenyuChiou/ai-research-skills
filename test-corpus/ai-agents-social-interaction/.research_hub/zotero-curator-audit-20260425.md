# Zotero curation report (read-only)

Generated by `zotero-library-curator` on 2026-04-25 against the user's
real Zotero library + research-hub vault. **No writes performed.**

**Library**: 14772686 (`我的文獻庫`) · **Items audited**: 1102 obsidian
notes (research-hub vault) + 500-tag sample from Zotero local API ·
**Source**: research-hub dedup index
(`~/knowledge-base/.research_hub/dedup_index.json`, 16,971 lines, 775
unique DOIs / 1100 unique titles) + Zotero local API
(http://localhost:23119/api/users/0/tags?limit=500).

## Duplicate DOIs (10)

The dedup index found 10 DOIs that appear in two or more Obsidian notes.
First three shown verbatim; full list reproducible by running:

```bash
python -c "import json; d=json.load(open('~/knowledge-base/.research_hub/dedup_index.json',encoding='utf-8'));
print([(k,len(v)) for k,v in d['doi_to_hits'].items() if isinstance(v,list) and len(v)>1])"
```

| DOI | Hits | Note |
|---|---|---|
| `10.18174/sesmo.2020a16340` | 2 | Same paper (Schwarz 2020 multi-theory survey) duplicated across `abm-theories/` and `llm-agents-behavioral-science-environmental-interaction/` clusters. **Suggested:** keep the `abm-theories` copy (better-tagged), merge other into it. |
| `10.48550/arXiv.2511.12319` | 2 | Same paper (Corazzini 2025 Decision/Gender Biases LLMs) in `llm-agents-behavioral-science-environmental-interaction/` and `llm-agents-social-cognitive-simulation/` clusters with slightly different titles ("Large Language Models" vs "LLMs"). **Suggested:** keep the version with `zotero_key=KQ69R8AT`, merge the other (no zotero_key). |
| `10.1057/s41599-024-03611-3` | 2 | Same paper (Gao 2024 LLM-empowered ABM survey) in `llm-agents-behavioral-science-environmental-interaction/` (key FZU23V6U) and `llm-agents-software-engineering/` (key AWS4TGJA). Two real Zotero items — not just notes. **Suggested:** decide canonical cluster (probably the survey one), then `research-hub clusters rebind`. |

Plus 7 more (full audit available via the Python one-liner above).

## Tag hygiene — case-only near-duplicates (44 pairs)

Real findings from the live Zotero `/api/users/0/tags` endpoint. First
10 shown:

| Tag A | Tag B | Likely cause |
|---|---|---|
| `ABM-Methodology` | `ABM-methodology` | inconsistent capitalisation when bulk-tagging |
| `Agent-Based Modeling` | `Agent-based modeling` | same |
| `Artificial Intelligence` | `Artificial intelligence` | same |
| `Best Management Practice` | `Best management practice` | same |
| `Decision Making` | `Decision making` | same |
| `Disaster Recovery` | `Disaster recovery` | same |
| `Flood Management` | `Flood management` | same |
| `Adaptation` | `adaptation` | same |
| `Agent-based model` | `agent-based model` | same |
| `Agent-Based Modeling` | `agent-based modeling` | same |

(34 more pairs not shown; same pattern — case-only differences.)

**Suggested fix:** consolidate to lowercase canonical form. Cannot be
done from the curator (read-only); requires either:
- Manual rename in Zotero desktop (one tag at a time, slow)
- `zotero-skills` batch-update via Web API (apply layer)

## Sparse tags (435 of 488 unique tags used by < 3 items)

89% of tags are used only by 1–2 items. Many are likely typos or
one-off ad-hoc tags that should either be promoted to a controlled
vocabulary or removed.

**Suggested fix:** human review. Out of scope for the curator; it can
flag but not decide.

## Cluster bloat / sparsity (research-hub vault)

From `python -m research_hub status`:

| Cluster | Items | Verdict |
|---|---|---|
| `llm-agents-social-cognitive-simulation` | 423 | **bloat — consider splitting** |
| `survey` | 237 | **bloat — consider splitting** (or rename to be more specific) |
| `llm-agents-software-engineering` | 168 | borderline |
| `traditional-abm` | 117 | borderline |
| (8 more clusters with 5–75 items each) | | OK |
| `ai-agents-social-interaction-test` | 5 | **orphan — leftover from skill verification, see below** |

## Orphan cluster + unassigned notes (real audit findings)

- **Cluster `ai-agents-social-interaction-test`** still appears in
  `research-hub status` with 5 papers, but `~/knowledge-base/raw/ai-agents-social-interaction-test/`
  no longer exists on disk (was deleted into
  `_deleted_ai-agents-social-interaction-test`). The cluster registry
  (`clusters.yaml`) is **out of sync with the obsidian filesystem**.
- **Unassigned notes (no `topic_cluster`)**: 4. These are obsidian
  notes that exist on disk but have no cluster binding — they won't
  appear in any cluster operation.

**Suggested fix:** run `research-hub clusters scaffold-missing` (idempotent)
to refresh registry, or `research-hub clusters delete ai-agents-social-interaction-test --apply --force` again followed by manual `rm -rf` of the
`_deleted_*` directory and the residual `bundles/` + `artifacts/` folders.

## Suggested follow-ups (in priority order)

1. **Consolidate the 10 duplicate DOIs** — easiest mechanical win;
   merge clusters or use `research-hub clusters rebind`. Mid effort,
   high value.
2. **Lowercase the 44 case-only tag pairs** via `zotero-skills` batch
   update. Mid effort, mid value (clean searches).
3. **Clean up the orphan `ai-agents-social-interaction-test` cluster
   + residual files**. Small effort, removes confusion.
4. **Defer**: 435 sparse tags + 2 bloat clusters → human review
   session, not auditable mechanically.

## Skill output footer

```
[zotero-library-curator]
  Library: 14772686 (我的文獻庫)
  Items audited: 1102 (research-hub vault) + 500-tag sample (Zotero API)
  Duplicate DOIs: 10
  Case-only near-duplicate tag pairs: 44
  Sparse tags (<3 items): 435 of 488 (89%)
  Cluster bloat candidates: 2 (>200 items)
  Orphan cluster found: 1 (ai-agents-social-interaction-test, 5 papers)
  Read-only — no writes performed.
  Suggested next: priority follow-ups above; defer to zotero-skills /
  research-hub clusters / manual review for any apply step.
```
